# This workflow is disabled - using deploy.yml + build.yml instead
name: "[Disabled] Deploy Hugo site to Pages"
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml instead."
